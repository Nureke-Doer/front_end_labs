# Lab 03: Components and Hooks
**Student:** Nureke

## Lab 3.1
Step Counter with independent states. Uses `useState` to track count, history, and operation count.

## Lab 3.2
Data fetching from JSONPlaceholder API. Implements `AbortController` for cleanup and optimized re-fetching via dependency arrays.
# Lab 5.2
Здесь я использовал `React.ChangeEvent<HTMLInputElement>` вместо `any`, чтобы TypeScript мог подсказывать свойства события и защищать от ошибок при доступе к `event.target.value`. 
Явно указывать дженерик `useState<User[]>` необходимо, когда начальное состояние — это пустой массив, иначе TypeScript не сможет сам догадаться, массив каких данных мы там храним.
# Lab 5.1
В этом задании я использовал `interface` для описания объектов User и Skill, потому что они определяют форму данных. Для `SkillLevel` я использовал `type alias` (Union type), так как это набор конкретных строковых значений, а не объект.
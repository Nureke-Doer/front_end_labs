1. Использованный инструмент:
Google Gemini

2. Запросы:

Просьба объяснить выполнение Lab 5.1 и 5.2 пошагово и без сленга, так как я начинающий программист.

Запросы на помощь в расшифровке ошибок терминала (например: "ошибки 'Cannot find module ./types'", "'React is declared but its value is never read'", "must be imported using a type-only import").

Инструкции по правильной заливке готового проекта на GitHub через терминал.

3. Как я изменял и проверял код:

Я набирал весь код вручную, не используя функцию copy-paste, чтобы соблюсти правила OquLabs.

Я проверял работоспособность приложения на каждом этапе, запуская локальный сервер командой npm run dev и тестируя интерфейс в браузере.

При возникновении ошибок компиляции я самостоятельно (с подсказками ИИ) исправлял структуру файлов (переносил UserCard.tsx и types.ts в правильную папку src), добавлял пропущенные ключевые слова export и дописывал функцию filter для корректной работы поиска.

4. Чему я научился:

Я понял фундаментальную разницу между файлами .ts (используются только для описания типов и логики) и .tsx (используются для отрисовки React-компонентов).

Научился использовать interface для описания структуры объектов (пользователи, навыки) и дженерики в useState<User[]> для типизации пустых массивов.

Узнал, как правильно типизировать события в React: React.ChangeEvent для поля ввода и React.MouseEvent для кнопок.

Разобрался со строгим режимом Vite: понял, почему нужно использовать import type при импорте интерфейсов.

Освоил базовый цикл работы с Git в консоли (add, commit, push) и узнал, что такое предупреждения о LF/CRLF.